const User = require('./User');
const Comments = require('./Comments');
const Like = require('./Like');
const Profile = require('./Profile');
const Post = require('./Post');

module.exports = { User, Comments, Like, Profile, Post };
